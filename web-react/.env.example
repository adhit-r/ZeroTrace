# ZeroTrace Frontend Environment Variables

# API Configuration
# Backend API base URL (default: http://localhost:8080)
VITE_API_URL=http://localhost:8080

# Enrichment Service URL (optional, if different from API)
VITE_ENRICHMENT_URL=http://localhost:8000

# Network Scanner Configuration
# Comma-separated list of default scan targets
VITE_DEFAULT_SCAN_TARGETS=192.168.1.0/24

# Default scan type (tcp, udp, syn)
VITE_DEFAULT_SCAN_TYPE=tcp

# Scan timeout in seconds
VITE_SCAN_TIMEOUT=30

# Scan concurrency (number of parallel scans)
VITE_SCAN_CONCURRENCY=10

# Development Mode
# Set to 'true' to enable debug logging
VITE_DEBUG=false
